import javax.swing.*;
import java.awt.*;


public class PanelDraftBoard extends JPanel{
    PanelDraftBoard(){
        this.setPreferredSize(new Dimension(500, 100));
        this.setBackground(Color.black);
        this.setFocusable(true);
    }
}
